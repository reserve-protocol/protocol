pragma solidity 0.8.4;

contract AuctionManager {
    function launchAuction(
        address sellingToken, 
        address buyingToken, 
        uint256 sellAmount
    ) external override {
        // TODO 
    }
}
