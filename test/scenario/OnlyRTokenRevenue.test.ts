// // The RToken retains 100% of collateral appreciation
// describe(`100% of the cash flow to the RToken - P${IMPLEMENTATION}`, () => {
//   beforeEach(async () => {
//     // Set up basket of only appreciating assets
//     // Issue RToken
//   })
//   it('should decrease quantities immediately after appreciation while maintaining >= ref units', async () => {})
//   it('should melt revenue RToken slowly after appreciation', async () => {})
//   it('should sell single assets for RToken', async () => {})
// })
