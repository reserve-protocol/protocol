// // Should support collateral types with pegs other than the identity (target/ref != 1)
// describe(`The peg (target/ref) should be arbitrary - P${IMPLEMENTATION}`, () => {
// for each of i in [1e-9, 0.5, 2, 1e9] ({
//   beforeEach(async () => {
//     // Set up basket of equal parts 2 fiatcoins:
//     // -  1 with a (target/ref) of i
//     // -  1 with a (target/ref) of one
//   })
//   it('should respect differing scales during issuance', async () => {})
//   it('should respect differing scales during redemption', async () => {})
//   it('should remain stable and not become undercapitalized or produce revenue', async () => {})
// })
// })
