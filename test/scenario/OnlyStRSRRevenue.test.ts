// // The RToken has no appreciation while the StRSR pool collects 100% of revenue
// describe(`100% of the cash flow to StRSR - P${IMPLEMENTATION}`, () => {
//   beforeEach(async () => {
//     // Set up basket of only appreciating assets
//     // Issue RToken
//   })
//   it('should decrease quantities immediately after appreciation while maintaining >= ref units', async () => {})
//   it('should sell newly minted RToken for RSR', async () => {})
//   it('should sell single assets for RSR', async () => {})
// })
