// // Our protocol should contain the SetProtocol as a subset of functionality
// describe(`Only non-appreciating collateral - P${IMPLEMENTATION}`, () => {
//   beforeEach(async () => {
//     // Set up basket of tokens against themselves, or pegged versions of themselves
//     // Issue RToken
//   })
//   it('should not produce revenue', async () => {})
//   it('should maintain a constant issuance basket', async () => {})
//   it('should appreciate when altruists/idiots donate', async () => {})
// })
